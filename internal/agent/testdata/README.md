Captured registration shapes from Codex CLI 0.156.1 and Claude Code 2.1.281 on
2026-09-23, using temporary `CODEX_HOME` / `CLAUDE_CONFIG_DIR` directories:

```text
codex mcp add data-mate-fixture -- /nonexistent/data-mate mcp bridge --root "/tmp/example root"
claude mcp add --transport stdio --scope user data-mate-fixture -- /nonexistent/data-mate mcp bridge --root "/tmp/example root"
```

Claude's unrelated first-run machine/application metadata is omitted. These
fixtures exercise passive decoding and preservation of a foreign registration
through the owning adapter lifecycle regression. Native acceptance additionally
runs the actual installed add/remove commands and new client sessions.
